<launch>
  <!-- Pre-processing package for filtering -->

    <node
      pkg="tof_processing"
      type="tof_processing_node.py"
      name="tof_processing_node"
      output="screen">

      <rosparam file="$(find tof_processing)/params/tof_processing_params.yaml"/>

    </node>

</launch>